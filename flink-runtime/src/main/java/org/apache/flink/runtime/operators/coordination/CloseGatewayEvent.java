package org.apache.flink.runtime.operators.coordination;

/** An operator event that closes the gateway between an operator coordinator and its subtask. */
public class CloseGatewayEvent implements OperatorEvent {}
